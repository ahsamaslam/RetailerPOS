@page
@model Retailer.POS.Web.Pages.ItemType.IndexModel
@inject Retailer.POS.Web.Services.IApiClient ApiClient

<h2>Item Type</h2>
<a asp-page="Create" class="btn btn-primary">Create New ItemType</a>

<table class="table mt-2">
    <thead><tr><th>Id</th><th>Name</th><th>Actions</th></tr></thead>
    <tbody>
        @foreach (var c in Model.ItemType)
        {
            <tr>
                <td>@c.Id</td>
                <td>@c.Name</td>
                <td>
                    <a asp-page="Edit" asp-route-id="@c.Id" class="btn btn-sm btn-warning">Edit</a>
                    <a asp-page="Details" asp-route-id="@c.Id" class="btn btn-sm btn-info">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>
