@page
@{
    ViewData["Title"] = "Create";
    Layout = "_EmptyLayout";
}

@model Retailer.POS.Web.Pages.SubGroups.IndexModel
@inject Retailer.POS.Web.Services.IApiClient ApiClient
<h2>Sub Groups</h2>
<div class="row mb-3">
    <div class="col-md-6">
        <input id="searchBox" class="form-control" placeholder="Search by name..." />
    </div>
    <div class="col-md-6">
        <a asp-page="Create" class="btn btn-primary mb-2">Create New Sub Group</a>
    </div>
</div>


<table id="myTable" class="table mt-2">
    <thead><tr><th>Id</th><th>Name</th><th>Actions</th></tr></thead>
    <tbody>
        @foreach (var s in Model.SubGroups)
        {
            <tr>
                <td>@s.Name</td>
                <td>@s.GroupName</td>
                <td>
                    <a asp-page="Edit" asp-route-id="@s.Id" class="btn btn-sm btn-warning">Edit</a>
                    <a asp-page="Details" asp-route-id="@s.Id" class="btn btn-sm btn-info">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<!--end::App Main-->
@section Scripts {
    <script>
        $(document).ready(function () {
            $('#myTable').DataTable({
                "paging": true,
                "searching": true,
                "ordering": true,
                "info": true
            });
        });
    </script>
}
